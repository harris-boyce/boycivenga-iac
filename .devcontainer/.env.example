# Environment Variable Overrides for UniFi Integration Testing
#
# This file documents available environment variables that can be overridden
# for local development. To use custom values, copy this file to `.env` in
# the same directory and modify as needed.
#
# Docker Compose will automatically load `.env` if present.

# Terraform UniFi Provider Configuration
# These are auto-configured in docker-compose.yml but can be overridden
#TF_VAR_unifi_username=admin
#TF_VAR_unifi_password=unifi-integration-test-password
#TF_VAR_unifi_api_url=https://localhost:8443
#TF_VAR_unifi_allow_insecure=true

# MongoDB Configuration (UniFi dependency)
# Only override if you need custom MongoDB settings
#MONGO_USER=unifi
#MONGO_PASS=unifi-mongo-password
#MONGO_HOST=mongodb
#MONGO_PORT=27017
#MONGO_DBNAME=unifi

# UniFi Container Settings
# Adjust memory limits if needed (values in MB)
#MEM_LIMIT=1024
#MEM_STARTUP=1024

# Integration Test Flags
# Set to "false" to disable integration tests
#UNIFI_INTEGRATION_TESTS_ENABLED=true
#UNIFI_TEST_CONTROLLER_URL=https://localhost:8443
